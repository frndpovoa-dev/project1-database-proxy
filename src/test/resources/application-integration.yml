app:
  grpc:
    port: 9090
  ignite:
    url: jdbc:ignite:thin://${IGNITE_HOST}:${IGNITE_PORT}
    show-sql: true

server:
  port: 8080
  servlet:
    context-path: /project1

management:
  server:
    port: 8081
