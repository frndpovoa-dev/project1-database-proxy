_pod_name: localhost

app:
  grpc:
    port: 9090
  db-proxy:
    hostname: localhost
    port: 9090
  db-proxy-datasource:
    url: jdbc:postgresql://localhost:5432/postgres?user=postgres&password=postgres

server:
  port: 8050
  servlet:
    context-path: /project1

management:
  server:
    port: 8051

logging:
  level:
    root: info
    dev.frndpovoa.project1: debug
