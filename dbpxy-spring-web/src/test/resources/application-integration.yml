app:
  db-proxy:
    hostname: localhost
    port: 9090
  db-proxy-datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    props:
      user: postgres
      password: postgres

server:
  port: 9091

logging:
  level:
    root: info
    com.dbpxy: debug
